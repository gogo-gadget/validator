run:
  skip-dirs-use-default: true
  timeout: 1m

service:
  golangci-lint-version: 1.31.x

linters:
  enable:
    - gofmt
    - golint
    - gosec
    - misspell
    - goconst
    - dogsled
    - bodyclose
    - unconvert
    - interfacer